// Placeholder Prisma export for compatibility with legacy tests and imports.
// Runtime data access in this codebase uses Drizzle via `@/lib/db`.
export const prisma = {} as Record<string, unknown>;
